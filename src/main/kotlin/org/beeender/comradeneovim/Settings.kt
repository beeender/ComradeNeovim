package org.beeender.comradeneovim

class Settings {
    /** Should ComradeNeovim connect to the opened nvim instances automatically. */
    var autoConnect = true
    /** Should the IDE show the nvim buffer in an editor. */ 
    var showEditorInSync = true
}
